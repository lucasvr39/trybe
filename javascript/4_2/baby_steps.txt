
let numbers = [5, 9, 3, 19, 70, 8, 100, 2, 35, 27];

1) Nesse primeiro exercício, percorra o array imprimindo todos os valores nele contidos com a função console.log();

1. dado o array let, crie um laço for; 
2. crie uma variável i que irá armazenar a posição dentro do array;
2. utilize o método console.log para imprimir passando o array como argumento junto da chamada do elemento da posição i. 

2) Para o segundo exercício, você deve somar todos os valores contidos no array e imprimir o resultado;

1. dado o array let, crie uma variável que irá armazenar o valor da soma;
2. crie um laço for e uma variável i que irá armazenar a posição dentro do array;
3. atualize a variável sempre a incrementando com o valor anterior + o atual elemento do array no loop;
4. fora do loop, imprima a variável da soma.


3) Para o terceiro exercício, calcule e imprima a média aritmética dos valores contidos no array;

1.dado o array let, crie uma variável de soma com valor 0;
2. crie um laço for e uma variável i que irá armazenar a posição dentro do array;
3. atualize a variável sempre a incrementando com o valor anterior + o atual elemento do array no loop;
4. divida a variável pelo array.lenght;
5. imprima o resultado.

4) Com o mesmo código do exercício anterior, caso valor final seja maior que 20, imprima a mensagem: "valor maior que 20". Caso não seja, imprima a mensagem: "valor menor ou igual a 20";

1. dado o array let, crie uma variável de soma com valor 0;
2. crie um laço for e uma variável i que irá armazenar a posição dentro do array;
3. atualize a variável sempre a incrementando com o valor anterior + o atual elemento do array no loop;
4. divida a variável pelo array.lenght;
5. crie uma condicional que irá imprir a frase "valor maior que 20" se o resultado se for maior de 20;
6. caso não atenda a condição, imprima "valor menor que 20".

5) Utilizando for, descubra qual o maior valor contido no array e imprima-o;

1. dado o array let, crie uma variável com valor 0 que irá armazenar o maior valor do array;
2. crie um laço for com uma variável que irá iterar pelo comprimento do array;
3. crie uma condicional que irá testar o valor do elemento atual do array de acordo com a iteração;
4. armazene na variável criada inicialmente caso o valor do elemento seja maior que o valor armazenado anteriormente na mesma variável;
5. após toda a iteração, imprima a variável criada para armazenar o maior valor do array.


6) Descubra quantos valores ímpares existem no array e imprima o resultado. Caso não exista nenhum, imprima a mensagem: "nenhum valor ímpar encontrado";

1. dado o array let, crie uma variável que será incrementada a cada primo com valor 0;
2. crie um laço for que irá iterar pelo comprimento do array;
3. crie uma condicional que testará se o elemento atual do array é primo;
4. quando o elemento for primo, ele incrementará a variável criada inicialmente em 1;
5. quando não for, ele não fará nada e pulará para o próximo elemento;
6. crie uma condição que testa se a variável criada no ínicio é maior que 0;
7. imprima "nenhum primo encontrado" quando a variável for igual a 0 e imprima o valor armazenado quando for maior.

7) Utilizando for, descubra qual o menor valor contido no array e imprima-o

1. dado o array let, crie uma variável com valor 0 que irá armazenar o menor valor do array;
2. crie um laço for com uma variável que irá iterar pelo comprimento do array;
3. crie outro laço for que irá iterar com a variável criada no laço anterior para percorrer o array;
4. dentro do segundo laço, crie uma condicional que testa se o elemento atual do array é menor que os anterior;
5. quando for, ele armazena na variável criada no ínicio do programa;
6. depois de percorrer o array, imprima a variável criada para armazenar o menor valor do array.